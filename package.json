{
  "name": "js-modular",
  "version": "1.0.0",
  "description": "still learning how to JS",
  "main": "index.html",
  "scripts": {
    "start-old": "http-server ./www",
    "build": "rollup --config",
    "start": "rollup --config --watch",
    "sass": "node-sass --source-map true --watch src/scss -o www/css --recursive  --output-style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^5.0.0",
    "rollup": "^2.35.1",
    "rollup-plugin-livereload": "^2.0.0",
    "rollup-plugin-serve": "^1.1.0",
    "rollup-plugin-terser": "^7.0.2"
  }
}
